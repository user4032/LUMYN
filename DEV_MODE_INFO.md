# Режим розробки (Development Mode)

## Налаштування

У файлі `.env` встановлено:
```
AUTH_DEV_CODE=true
```

## Як працює реєстрація в режимі розробки

### 1. **Автоматичний показ коду верифікації**

Коли `AUTH_DEV_CODE=true`:
- Система **завжди** повертає код верифікації у відповіді
- Навіть якщо email не відправився (проблеми з SMTP), користувач все одно отримає код
- Код відображається на екрані з позначкою "✅ DEV код: 123456"

### 2. **Що бачить користувач**

При реєстрації після введення даних:
```
Введіть код, який ми відправили на вашу пошту

✅ DEV код: 123456

(Режим розробки - код показано тут)
```

### 3. **Логування на сервері**

При старті сервера з'являється повідомлення:
```
✅ Server running on port 4777
   Health check: http://localhost:4777/health
   Environment: development
   🔧 DEV MODE: Verification codes will be shown in responses
```

При реєстрації/повторній відправці коду:
```
[DEV] Verification code for user@example.com: 123456
[DEV] Email sent successfully to user@example.com
```

Або якщо email не відправився:
```
[DEV] Verification code for user@example.com: 123456
[DEV] Failed to send email, but returning code anyway: SMTP is not configured
```

### 4. **Налаштування SMTP (опціонально)**

Якщо хочете щоб email реально відправлявся:

```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=ваш_email@gmail.com
SMTP_PASS=пароль_додатку_gmail
SMTP_FROM=LUMYN
```

**Отримання пароля додатку для Gmail:**
1. Увімкніть 2-Factor Authentication в Google
2. Перейдіть на https://myaccount.google.com/apppasswords
3. Створіть новий пароль додатку
4. Скопіюйте його в `SMTP_PASS`

### 5. **Продакшн режим**

Для продакшну встановіть:
```env
AUTH_DEV_CODE=false
```

У цьому режимі:
- Email **обов'язково** має відправитися
- Якщо email не відправився - користувач отримає помилку
- Код верифікації **не** показується у відповіді

## Переваги режиму розробки

✅ Швидке тестування реєстрації без налаштування email
✅ Не потрібно перевіряти пошту щоразу
✅ Працює навіть при проблемах з SMTP
✅ Зручно для команди розробки

## Безпека

⚠️ **ВАЖЛИВО:** Завжди встановлюйте `AUTH_DEV_CODE=false` на продакшн серверах!

