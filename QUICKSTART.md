# DisgRAM - Швидкий старт

## ⚠️ Перш ніж почати

### Встановіть Node.js

1. **Завантажте Node.js**: https://nodejs.org
2. Виберіть версію **LTS** (Long Term Support) - рекомендовано v20 або v18
3. Запустіть інсталятор та слідуйте інструкціям
4. **Перезапустіть VS Code** після встановлення

### Перевірте встановлення

Після встановлення Node.js, відкрийте новий термінал PowerShell та виконайте:

```powershell
node --version
```
Має показати версію Node.js (наприклад, v20.11.0)

```powershell
npm --version
```
Має показати версію npm (наприклад, 10.2.4)

## 🚀 Встановлення проекту

Після успішного встановлення Node.js:

### 1. Встановіть залежності

```powershell
npm install
```

Це завантажить та встановить всі необхідні пакети:
- React та ReactDOM
- Electron
- Redux Toolkit
- Material-UI
- TypeScript
- Vite та інші інструменти

⏱️ **Очікуваний час**: 2-5 хвилин (залежить від швидкості інтернету)

### 2. Запустіть проект

```powershell
npm run dev
```

Це:
- ✅ Запустить Vite dev server
- ✅ Автоматично відкриє Electron вікно
- ✅ Включить hot reload
- ✅ Відкриє DevTools для розробки

### 3. Якщо виникають помилки

#### Помилка "node is not recognized"
- Node.js не встановлено або не додано до PATH
- Перезапустіть VS Code після встановлення Node.js
- Можливо потрібно перезавантажити комп'ютер

#### Помилка при npm install
```powershell
# Очистіть кеш npm
npm cache clean --force

# Спробуйте встановити заново
npm install
```

#### Помилка доступу (EACCES/EPERM)
```powershell
# Запустіть PowerShell як адміністратор
# Або встановіть залежності в іншій папці
```

## 📋 Наступні кроки

Після успішного запуску:
1. ✅ Проект працює в режимі розробки
2. 📝 Можна редагувати код - зміни відобразяться автоматично
3. 🎨 Налаштувати UI під свої потреби
4. 🔌 Підключити backend (у майбутньому)

## 🆘 Потрібна допомога?

Якщо виникли проблеми:
- Перевірте чи встановлений Node.js: `node --version`
- Переконайтеся що ви в правильній папці: `cd e:\yes\disgram`
- Видаліть node_modules та спробуйте знову: `rm -r node_modules; npm install`

---

**Готові до розробки? Встановіть Node.js та запустіть `npm install`! 🚀**
