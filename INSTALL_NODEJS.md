# Інструкція з встановлення Node.js для DisgRAM

## ⚠️ Node.js НЕ ВСТАНОВЛЕНИЙ

Якщо ви бачите ярлик в Start Menu, це не означає що Node.js встановлений.

## 📥 Спосіб 1: Офіційний інсталятор (РЕКОМЕНДОВАНО)

### Крок 1: Завантажте Node.js

1. Відкрийте: https://nodejs.org/uk
2. Завантажте **LTS версію** (зеленою кнопка)
3. Виберіть Windows Installer (.msi) для вашої системи:
   - Для 64-bit: node-v20.x.x-x64.msi
   - Для 32-bit: node-v20.x.x-x86.msi

### Крок 2: Встановіть Node.js

1. **Запустіть** завантажений .msi файл
2. Натисніть **Next** на екрані привітання
3. Прийміть ліцензійну угоду
4. **ВАЖЛИВО**: Залиште всі галочки як є (особливо "Add to PATH")
5. Натисніть **Install**
6. Дочекайтеся завершення (1-2 хвилини)
7. Натисніть **Finish**

### Крок 3: Перевірте встановлення

1. **ЗАКРИЙТЕ VS Code повністю**
2. **Відкрийте VS Code заново**
3. Відкрийте термінал PowerShell
4. Виконайте:

```powershell
node --version
```

Має показати: `v20.11.0` (або схожу версію)

```powershell
npm --version
```

Має показати: `10.2.4` (або схожу версію)

### Крок 4: Встановіть залежності проекту

```powershell
cd e:\yes\disgram
npm install
```

### Крок 5: Запустіть проект

```powershell
npm run dev
```

---

## 📥 Спосіб 2: Через Chocolatey (Для досвідчених)

Якщо у вас встановлений Chocolatey package manager:

```powershell
# Запустіть PowerShell як Адміністратор
choco install nodejs-lts -y

# Перезапустіть VS Code
```

---

## 📥 Спосіб 3: Через winget (Windows 10/11)

```powershell
# Запустіть PowerShell як Адміністратор
winget install OpenJS.NodeJS.LTS

# Перезапустіть VS Code
```

---

## 🐛 Якщо виникли проблеми

### "node is not recognized" після встановлення

**Рішення 1: Перезапустіть комп'ютер**
- Найпростіший спосіб оновити PATH

**Рішення 2: Додайте до PATH вручну**

1. Натисніть `Win + R`
2. Введіть: `sysdm.cpl` та Enter
3. Вкладка "Advanced" → "Environment Variables"
4. У "System variables" знайдіть "Path" → Edit
5. Додайте: `C:\Program Files\nodejs\`
6. OK → OK → OK
7. Перезапустіть VS Code

**Рішення 3: Перевірте шлях встановлення**

```powershell
# Знайдіть де встановлений Node.js
Get-ChildItem -Path "C:\Program Files" -Filter "node.exe" -Recurse -ErrorAction SilentlyContinue
Get-ChildItem -Path "C:\Program Files (x86)" -Filter "node.exe" -Recurse -ErrorAction SilentlyContinue
```

### Помилка доступу при встановленні

- Запустіть інсталятор **від імені адміністратора**
- Права клік → Run as Administrator

---

## ✅ Після успішного встановлення

Перейдіть до [QUICKSTART.md](QUICKSTART.md) для запуску проекту!

---

**Потрібна додаткова допомога?** Напишіть яка помилка виникла.
